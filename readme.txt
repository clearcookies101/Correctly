Dependences:
sudo apt update
sudo apt install python3-pip -y
pip3 install requests msal language-tool-python python-docx docx2txt
sudo apt install default-jre -y (Optional)

To run (may vary depending on what you have installed): 
python main.py
python3 main.py

After you run the file it will ask you to click a link. Then, you must put in a security code and accept the following prompts.
After you do so you should see a corrected file in the folder with your name.